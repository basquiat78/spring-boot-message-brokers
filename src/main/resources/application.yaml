spring:
  application:
    name: message-brokers-server
  profiles:
    active: {SPRING_PROFILES_ACTIVE:local}

server:
  error:
    whitelabel:
      enabled: false

app:
  messaging:
    use-amqp: true
    use-redis: true
    use-kafka: true